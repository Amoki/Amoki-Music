machine:
  python:
    version: 3.4.3

dependencies:
  override:
    - pip install -r requirements_dev.txt
    - pip install coveralls

test:
  override:
    - coverage run manage.py test
  post:
    - coveralls
